import { StatutAnnoncePipe } from './statut-annonce.pipe';

describe('StatutAnnoncePipe', () => {
  it('create an instance', () => {
    const pipe = new StatutAnnoncePipe();
    expect(pipe).toBeTruthy();
  });
});
